# Maintainer: Hector Martin <marcan@marcan.st>

pkgname=asahi-scripts
pkgver=20220918
pkgrel=1
pkgdesc='Asahi Linux maintenance scripts'
arch=('any')
url='http://asahilinux.org'
license=('MIT')
source=("${pkgname}-${pkgver}.tar.gz::https://github.com/AsahiLinux/${pkgname}/archive/refs/tags/${pkgver}.tar.gz")
sha256sums=('dfa2d432ee40a673a3eb0fd467141473f3f86c96406b79f07839fab114d38cbc')
b2sums=('e345bbfc3571a402155fcfea4d345443ddb6225df567fcddf07af0e6a7731f54b244233c3b1afdbdf07f19e232d811648ce5611335f4a1a8dde71b5ee4fff524')
backup=(etc/m1n1.conf)

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make PREFIX=/usr DESTDIR=${pkgdir} install

  install -Dm644 "$srcdir/${pkgname}-${pkgver}/LICENSE" \
    "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
